"0","# Stratify by sex and race, and normalize within each stratum
for(method in step_methods) {
  dt.all <- dt.all %>%
    group_by(Sex, Race) %>%
    mutate(!!paste0(method, """") := scale(!!sym(method))) %>%
    ungroup()
}
"
"2","Error: object 'step_methods' not found
"
